# 📋 연결 승인 프로세스 구현 및 검증 계획서

본 문서는 BLE 연결 시 수신 측(Peripheral)에서 사용자의 승인을 거쳐 채팅방에 진입하도록 하는 보안 및 UX 개선 기능의 구현 계획을 정리합니다.

---

## 1. 🎯 구현 목표
*   **연결 제어 권한 부여:** 광고(Advertising) 중인 기기가 예기치 않은 상대방과 연결되는 것을 방지.
*   **수동 채팅방 진입:** 물리적 연결 직후 바로 채팅방에 들어가는 대신, 사용자가 '수락'을 눌렀을 때만 UI를 전환.
*   **일시적 거절 로직:** 거절 시 현재 연결을 끊되, 상대방이 다시 목록에서 찾아 시도할 수 있도록 허용.

---

## 2. 🛠 기술적 구현 계획

### 2.1 컨트롤러 로직 수정 (`ChatController.dart`)
1.  **연결 감지 시점 분기 처리:**
    *   현재: `CONNECTED` 이벤트 수신 시 즉시 채팅방 이동 및 스낵바 출력.
    *   변경: 
        *   `isPeripheralMode`가 `true`인 경우: 채팅방 이동을 보류하고 **승인 다이얼로그** 출력.
        *   `isPeripheralMode`가 `false`인 경우(Central): 상대방의 수락을 기다리는 대기 UI/상태 표시 (또는 기존처럼 진입 후 상대방 거절 시 쫓겨나는 로직).

2.  **승인 다이얼로그 (`Get.dialog`) 구현:**
    *   **수락 시:** `Get.toNamed('/chat')` 호출 및 대화 시작.
    *   **거절 시:** `_bleManager.disconnect()` 호출 후 스캔 결과 초기화.

### 2.2 네이티브 연동
*   이미 `BlePeripheralManager`와 `BleCentralManager`에 연결/해제 콜백이 구현되어 있으므로, Flutter 레이어의 로직 수정만으로 구현 가능.

---

## 3. 🧪 테스트 및 검증 절차

### 3.1 테스트 시나리오
| 번호 | 시나리오 | 예상 결과 |
| :--- | :--- | :--- |
| **Test 1** | Peripheral이 '수락' 클릭 | 양쪽 기기 모두 채팅방 진입 및 메시지 송수신 가능 확인 |
| **Test 2** | Peripheral이 '거절' 클릭 | Peripheral은 다이얼로그 닫힘, Central은 연결 종료 알림 수신 및 목록으로 복귀 |
| **Test 3** | 다이얼로그 중 Central이 먼저 취소 | Peripheral의 다이얼로그가 자동으로 닫히거나 연결 종료 로그 출력 확인 |
| **Test 4** | 거절 후 재연결 시도 | Central의 스캔 목록에 다시 나타나고 재연결이 가능해야 함 |

### 3.2 검증 기준 (Success Metrics)
*   [ ] 승인 전에는 절대 채팅방 UI가 나타나지 않음.
*   [ ] 거절 시 블루투스 연결이 즉시 끊어짐 (로그로 확인).
*   [ ] 거절 행위가 기기 차단(Blacklist)으로 이어지지 않고 '일시적'임을 보장.

---

## 4. ⚠️ 사이드 이펙트 분석 및 방어 전략

### 4.1 상태 불일치 (State Mismatch)
*   **문제:** Central(요청자)은 물리적으로 연결된 순간 `CONNECTED`로 판단하지만, Peripheral은 아직 '승인 대기' 상태임. 이때 Central이 메시지를 먼저 보내면?
*   **해결:** Peripheral이 수락하기 전까지 수신된 메시지는 버리거나, `messages` 리스트에 담아두었다가 수락 후 한꺼번에 보여줌. (이번 단계에서는 수락 전 메시지는 무시하는 방향 권장)

### 4.2 중복 다이얼로그 발생
*   **문제:** 연결 신호가 짧은 시간 내에 여러 번 들어올 경우 다이얼로그가 중첩되어 뜰 수 있음.
*   **해결:** `Get.isDialogOpen` 속성을 체크하여 이미 다이얼로그가 떠 있다면 추가로 띄우지 않도록 방어 코드 작성.

### 4.3 연결 유지 타임아웃
*   **문제:** 사용자가 다이얼로그를 띄워놓고 아무것도 누르지 않을 경우, OS 단에서 연결을 강제로 끊을 수 있음.
*   **해결:** 특별한 로직 추가보다는, 연결이 끊기면 자동으로 다이얼로그가 닫히도록 `connectionStream`에서 처리.

---

## 5. 📂 관련 파일
*   `lib/controller/chat_controller.dart`: 메인 로직 수정
*   `lib/ui/scan_page.dart`: 다이얼로그 UI 호출 위치
